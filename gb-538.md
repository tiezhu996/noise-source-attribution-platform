# gb-538 工业噪声源贡献归因

请生成一个名为 `industrial-noise-source-attribution` 的 Go 全栈 Web 项目，面向工业企业声学与环保团队，用于维护监测点和设备声源谱、导入倍频程测量结果、扣除背景噪声并计算各声源贡献排序。系统仅提供离线决策支持与仿真，不连接或控制真实采集器、生产设备或降噪装置；不得实现商城、电商、预约、订单、仓储库存、财务、客服工单、博客或大众社区。

## 1. 项目概述与复杂度下限

- 类型：B2B 前后端分离全栈 Web 应用。
- 必须实现 4 个核心实体、5 个核心页面、至少 3 个共享组件、至少 2 个 hooks、至少 4 个后端中间件和 2 组前后端一致的共享枚举。
- 实体必须贯穿 PostgreSQL、Go model/dto/repository/service/handler/router、Vue type/api/store/page。
- 本项目用于验证跨文件协同改动能力。严禁合并职责到单一文件；字段、状态、频带或算法版本变更必须触达至少 5 个文件。

## 2. 核心实体

1. `MonitoringPoint`（监测点）
   - 字段：`id`、`point_code`、`name`、`x_m`、`y_m`、`height_m`、`area_type`、`background_profile_json`、`owner_team`、`point_state`、`created_at`。
   - 关系与动作：拥有多次噪声测量并参与归因运行；支持建点、更新坐标/背景谱、停用和查看摘要。
   - 必须贯穿 `monitoring_points` 表 -> 独立 model/dto/repository/service/handler/router -> `frontend/src/types/monitoring-point.ts`、api、Pinia store 和页面。
2. `NoiseMeasurement`（噪声测量）
   - 字段：`id`、`monitoring_point_id`、`measured_at`、`duration_s`、`octave_bands_json`、`overall_dba`、`background_dba`、`weather_note`、`source_checksum`、`measurement_quality`、`quality_reason`、`imported_by`。
   - 关系与动作：隶属监测点并被归因运行冻结引用；支持导入、质量校验、背景标记、作废和重导。
   - 必须贯穿 `noise_measurements` 表及独立后端分层、前端 type/api/store/page 文件。
3. `SourceProfile`（声源谱）
   - 字段：`id`、`source_code`、`name`、`x_m`、`y_m`、`height_m`、`reference_distance_m`、`octave_power_json`、`directivity_json`、`operating_factor`、`profile_state`、`version`、`updated_at`。
   - 关系与动作：描述候选设备声源并被多次归因引用；支持建档、频谱版本、启用和废止。
   - 必须贯穿 `source_profiles` 表及独立后端分层、前端 type/api/store/page 文件。
4. `AttributionRun`（贡献归因运行）
   - 字段：`id`、`measurement_ids`、`source_profile_ids`、`algorithm_version`、`input_hash`、`normalized_bands_json`、`contributions_json`、`residual_error`、`attribution_state`、`explanation`、`started_at`、`finished_at`、`reviewed_by`。
   - 关系与动作：冻结测量和声源版本；支持排队、计算、比较、人工确认与作废，历史结果不可覆盖。
   - 必须贯穿 `attribution_runs` 表及独立后端分层、前端 type/api/store/page 文件。

## 3. 差异化算法与状态流

- 倍频程频带固定为 63/125/250/500/1000/2000/4000/8000 Hz；对总声级与背景声级做能量域相减，差值过小时标记为不可可靠扣除，禁止直接做 dB 算术减法。
- 按声源至监测点距离、方向性与运行系数计算频带衰减矩阵，使用成熟数值库完成非负最小二乘贡献拟合；输出每个频带贡献、总贡献、残差和不可辨识提示。
- `NoiseMeasurement`：`captured -> validated -> normalized -> ready`，不合格到 `rejected`，`ready -> superseded`；非法迁移返回 409。
- `AttributionRun`：`queued -> calculating -> completed/failed -> reviewed -> confirmed/voided`；同一输入哈希与算法版本幂等。
- 算法必须用已知合成数据夹具验证，不声称满足法定测量规范；结果不能替代专业声学报告或直接控制设备。

## 4. 核心页面与实体消费

1. `/points` 监测点：消费 `MonitoringPoint + NoiseMeasurement`，展示坐标、背景谱和最近质量。
2. `/measurements` 测量工作台：消费 `NoiseMeasurement + MonitoringPoint`，导入频谱并查看质量判断。
3. `/sources` 声源谱：消费 `SourceProfile + MonitoringPoint`，维护位置、参考距离和版本化频谱。
4. `/attribution` 贡献归因：消费 `AttributionRun + NoiseMeasurement + SourceProfile`，展示频带堆叠、贡献排序、残差和解释。
5. `/audit` 审计中心：消费四实体变更投影和审计日志，支持 request ID、实体、操作者、时间筛选。

`QualityBadge` 被监测点、测量和归因页共用；`OctaveBandChart` 被测量、声源和归因页共用；`AttributionDetailDrawer` 被归因、声源和审计页共用。

## 5. 横切关注点

- JWT + RBAC：角色至少 `admin`、`acoustic_engineer`、`data_analyst`、`reviewer`、`auditor`；联动角色表、JWT、Go auth/rbac、路由、Vue guard、按钮显隐和类型。运行发起者不得确认自己的结果。
- 审计与追踪：所有写操作保存前后快照、操作者和 request ID；算法保存输入 checksum、版本、频带参数、耗时和摘要。
- 全局错误：频带缺失/不一致返回 422，状态/版本冲突返回 409，权限返回 403，前端统一处理。
- 限流与幂等：登录、测量导入和归因接口分别限流；归因使用 `Idempotency-Key`。

## 6. 共享枚举、组件、hooks 与中间件

- `MeasurementQuality = valid | contaminated | clipped | missing`：后端 `backend/internal/constants/measurement_quality.go`，前端 `frontend/src/types/enums/measurement-quality.ts`。
- `AttributionState = queued | calculating | completed | failed | reviewed | confirmed | voided`：后端 `backend/internal/constants/attribution_state.go`，前端 `frontend/src/types/enums/attribution-state.ts`。
- README 列出枚举在 model、dto、算法、service 状态机、store、组件、页面和测试中的全部位置。
- 共享组件：`QualityBadge`、`OctaveBandChart`、`AttributionDetailDrawer`；hooks/composables：`useAuth`、`useAttributionRun`。
- 中间件至少拆分 `request_id.go`、`recovery.go`、`auth.go`、`rbac.go`、`audit.go`、`error_handler.go`。

## 7. 技术、规模与文件结构

- 英文短名：`industrial-noise-source-attribution`。
- 前端：Vue 3 + TypeScript + Vite + Element Plus + Pinia + ECharts；后端：Go 1.22 + Gin + GORM + Gonum；正式 PostgreSQL 16，runtime smoke 支持 SQLite；JWT。
- API 统一使用 `/api/v1`，另提供真实 `/healthz`；使用 validator/v10、统一响应和错误码、`%w` 错误链、request ID、结构化日志、构造器注入并实现优雅停机。多表写入用事务，状态迁移用条件更新，唯一性/幂等不能只靠前端。禁止 TODO、placeholder、空函数、硬编码成功响应和 handler 直连数据库。
- Go 功能代码 2500-4200 行、24-42 个功能 `.go` 文件且少于 5000 行；不计 `_test.go`、生成代码和 vendor。dB 能量换算、频带校验、拟合、状态机和权限须有测试。
- 只保留普通项目单测；不得创建名称含 record/regression/grading/scoring 的评分测试，不得加入答案、`BUG_REPRO.md` 或标注流水线内部文件。
- 根 `go.work` 纳入 `./backend`，支持根执行 `go work sync`、`go build ./backend/...`、`go test ./backend/...`。

```text
.
├── go.work  runtime_smoke.json
├── docker-compose.yml  .env  .env.example  .gitignore  README.md
├── output/
│   └── execution.md
├── backend/
│   ├── go.mod  Dockerfile  cmd/server/main.go
│   └── internal/
│       ├── config/ constants/ dto/ model/
│       ├── repository/ service/ handler/ router/
│       ├── middleware/ algorithm/ acoustics/ util/
├── frontend/
│   ├── package.json  Dockerfile  nginx.conf
│   └── src/
│       ├── api/ stores/ types/ components/common/
│       ├── hooks/ pages/ router/ utils/
└── database/init.sql
```

四实体必须在全部后端层及前端 api/store/type 中按实体分文件。严禁合并职责到单一文件，不得用通用 CRUD handler/store 取代实体模块。

## 8. 运行 Manifest、部署与验证交付

- 固定端口：前端 `18538`、后端 `19538`、PostgreSQL 宿主机 `57538`；容器内 `80/8080/5432`。
- 根 `runtime_smoke.json` 的唯一用途是提供可解析的启动 manifest，内容必须严格等价于：

```json
{
  "mode": "service",
  "start": "go run ./cmd/server",
  "workdir": "backend",
  "env": {
    "PORT": "20538",
    "DB_DRIVER": "sqlite",
    "DB_DSN": "file:runtime-smoke?mode=memory&cache=shared",
    "JWT_SECRET": "runtime-smoke-only-change-me"
  },
  "ready_url": "http://127.0.0.1:20538/healthz",
  "ready_status": [200],
  "startup_timeout": 30
}
```

- 后端按 manifest 自动建 SQLite 表和演示数据，正式 Compose 仍用 PostgreSQL；manifest 禁止出现 passed、截图或证据字段。
- Compose 顶层 `name: industrial-noise-source-attribution` 且无 `version:`；编排 db/backend/frontend，使用命名卷和英文短名容器名前缀，数据库、后端和前端均有 healthcheck，并按健康条件依赖。
- `.env`、`.env.example` 均含 `COMPOSE_PROJECT_NAME=industrial-noise-source-attribution`、`FRONTEND_PORT=18538`、`BACKEND_PORT=19538`、`DB_PORT=57538`、`DB_DRIVER=postgres`、DB 变量和 `JWT_SECRET`；`.env` 不提交。前端只访问 `/api`，Nginx 原样代理 `/api/v1` 到 `backend:8080`，另提供 `/api/healthz` 并支持 SPA。
- README 首屏给 `docker compose up -d`，说明项目介绍、主要功能、角色账号、架构与目录、API 清单、技术栈表、环境变量与端口、本地开发、Docker 部署、枚举位置、算法与安全边界、runtime smoke、构建测试、常见问题、停止方式和 License；README 不得承载执行证据。
- 必须真实执行 `go work sync`、`go build ./backend/...`、`go vet ./backend/...`、`go test ./backend/...`、`npm --prefix frontend ci`、`npm --prefix frontend run build`、`python3 /Users/gaobo/.codex/skills/go-annotation-pipeline/scripts/project_scale.py .`、`python3 /Users/gaobo/.codex/skills/go-annotation-pipeline/scripts/runtime_smoke.py .`、`docker compose config --quiet`、`docker compose up -d --build`、`docker compose ps`；前后端均须有 healthcheck且全部 healthy。至少 8 个真实 API 冒烟覆盖 health、登录、四实体、创建/导入、合法/非法迁移、归因运行/读取和 403。
- API 冒烟还必须包含详情读取、未认证 401 与越权 403；四个核心实体各至少经过一条真实后端链路，五个页面都消费真实 `/api/v1`，不得使用静态 mock。并在 `backend/` 内复跑 `go build ./...`、`go vet ./...`、`go test ./...`。
- 仅使用 Codex 内置 Browser 验证登录、监测点、测量、声源、归因五页，禁止外部 Chrome；实际完成创建或编辑、列表刷新和一次状态变化，并检查 console/network、文字溢出和遮挡。
- 所有构建、测试、runtime smoke、Compose、API、Browser、console/network 与关闭证据只能写 `output/execution.md`；截图必须直接位于 `output/`，至少桌面和移动端各一张并被 execution.md 引用，禁止在其他文件复制证据或伪造结果。
- `output/execution.md` 必须包含项目编号、验证时间、实际端口、规模实测、根与 backend 构建/vet/test、前端构建、服务 healthy、API 表、Browser 操作步骤、截图相对路径、逐条需求覆盖、修复摘要、停服无残留和实现提交 hash。
- 验证后执行 `docker compose down -v --remove-orphans`，确认容器、网络和命名卷均已清理，并停止 runtime smoke 进程。
- 项目必须独立 `git init`，设置本地身份 `git config user.name "blueship581"`、`git config user.email "brysj.hhrhl.g@gmail.com"`。真实实现与验证完成后提交 `git add -A && git commit -m "feat: implement and verify industrial-noise-source-attribution"`，把实现提交 hash 写入 `output/execution.md`，再执行 `git add output/execution.md && git commit -m "docs: record verified execution"`，形成两次提交。`.gitignore` 排除 `.env`、依赖、构建产物、日志和本地 SQLite 文件，但保留 execution.md 与截图；最终 `git status --short` 为空，不得共用 Git 仓库。
